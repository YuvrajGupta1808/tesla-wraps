{
  "name": "wrap-wizard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
